{%- assign theme_name = 'rawfeed' -%}

{%- comment -%} URLS {%- endcomment -%}

{%- assign project_url = 'https://rawfeed.github.io/rawfeed-jekyll' -%}
{%- assign home_url = '/' | relative_url -%}
{%- assign blog_url = '/blog/' | relative_url -%}
{%- assign pixels_url = '/pixels/' | relative_url -%}
{%- assign search_url = '/blog/?search=open' | relative_url -%}
{%- assign tags_url = '/blog/tags/' | relative_url -%}
{%- assign feed_url = '/feed.xml' | relative_url -%}
{%- assign pub_url = '/pub/' -%}

{%- comment -%} SCREEN {%- endcomment -%}

{%- assign generic = site.data.generic -%}
{%- assign blog = site.data.screen.blog.blog -%}
{%- assign contact = site.data.screen.contact.contact -%}
{%- assign donate = site.data.screen.donate.donate -%}
{%- assign footer = site.data.screen.footer.footer -%}
{%- assign head = site.data.screen.head.head -%}
{%- assign home = site.data.screen.home.home -%}
{%- assign maintenance = site.data.screen.maintenance.maintenance -%}
{%- assign navbar = site.data.screen.navbar.navbar -%}
{%- assign page_data = site.data.screen.page.page -%}
{%- assign pixels = site.data.screen.pixels.pixels -%}
{%- assign pub = site.data.screen.pub.pub -%}
{%- assign resume = site.data.screen.resume -%}
{%- assign socials = site.data.screen.socials.socials -%}

{%- comment -%} VALUES {%- endcomment -%}

{%- assign datelang = site.data.generic.datelang -%}
{%- assign default = site.data.generic.default -%}
{%- assign site_title = site.data.generic.title -%}
{%- assign author = site.data.generic.author -%}
