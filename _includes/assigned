{%- assign theme_name = 'rawfeed' -%}
{%- assign project_url = 'https://rawfeed.github.io/rawfeed-jekyll' -%}
{%- assign home_url = '/' | relative_url -%}
{%- assign blog_url = '/blog/' | relative_url -%}
{%- assign pixels_url = '/pixels/' | relative_url -%}
{%- assign search_url = '/blog/?search=open' | relative_url -%}
{%- assign tags_url = '/blog/tags/' | relative_url -%}
{%- assign feed_url = '/feed.xml' | relative_url -%}
{%- assign pub_url = '/pub/' -%}
{%- assign head = site.data.options.head -%}
{%- assign site_title = site.data.options.title -%}
{%- assign author = site.data.options.author -%}
{%- assign header = site.data.options.header -%}
{%- assign blog = site.data.options.blog -%}
{%- assign pixels = site.data.options.pixels -%}
{%- assign home = site.data.options.home -%}
{%- assign default = site.data.options.default -%}
{%- assign page_data = site.data.options.page -%}
{%- assign donate = site.data.options.donate -%}
{%- assign contact = site.data.options.contact -%}
{%- assign footer = site.data.options.footer -%}
{%- assign socials = site.data.options.socials -%}
{%- assign pub = site.data.options.pub -%}
{%- assign datelang = site.data.options.datelang -%}
{%- assign maintenance = site.data.options.maintenance -%}
{%- assign reading_time = site.data.options.reading_time -%}
